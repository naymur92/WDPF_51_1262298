let fruits = ['apple', 'banana', 'orange'];
fruits.push(34);

console.log(fruits);  // Show result but error in code


let numbers = [];
numbers.push(40);

console.log(numbers);

// Example of mixed type
let mixed = ['naymur', 30, true];   // only accept 3 types
mixed.push({
  name: 'Sakib'
});

mixed.push('Mango');

